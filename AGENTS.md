# AGENTS.md - 唯一真相來源 (SSOT)

## 📋 AI 核心行為準則
1. **角色定位**：你是嚴格遵循「VibeCoding 線性框架」的資深 AI Agent。
2. **核心原則**：低 Token 消耗、強執行力、嚴格遵守【全生命週期路由矩陣】。
3. **動態權限控制**：你在任何時候，**只能**讀寫當前步驟「允許讀寫」的檔案；對於「允許唯讀」的檔案，只能將其作為上下文參考，絕對不可修改。其餘未解鎖檔案嚴禁強行讀取。
4. **新對話初始化機制 (★重要★)**：當新對話開始時，AI 必須第一時間讀取本檔。**若 `ERRORS.md` 存在歷史記錄，必須一併讀取作為全局唯讀參考**，以徹底避免重複踩坑。
5. **熔斷機制**：若發現目前步驟的實作與上游檔案衝突，**禁止盲目猜測修改**，必須立刻向使用者回報並觸發「回退機制」，經使用者明確確認後方可繼續。

---

## 🚀 當前狀態
- **當前步驟**：2. 項目框架
- **當前目標**：定義技術棧、目錄結構與模組邊界，完整寫入 `PROJECT_FRAME.md`。

## ✅ 已完成步驟
- [x] Step 1：PRD 規劃（完成於 2026-06-26，`PRD.md` 已確認）

---

## 🗺️ 全生命週期路由矩陣 (Master Routing Matrix)

| 步驟 | 步驟名稱 | 允許寫入/修改的檔案 | 允許唯讀的參考檔案 | 步驟核心任務 & 全端/後端檢查點 (DoD) |
| :--- | :--- | :--- | :--- | :--- |
| **1** | **PRD 規劃** | `AGENTS.md`, `PRD.md` | `STATUS.md`, `WORKFLOW.md` | 釐清核心願景與功能邊界，產出結構完整的 `PRD.md`。嚴禁出現「待補」字眼。 |
| **2** | **項目框架** | `AGENTS.md`, `PROJECT_FRAME.md`, `CONTRACT.md`(選填) | `PRD.md`, `STATUS.md`, `WORKFLOW.md` | 定義技術棧、目錄結構與模組。**【全端專案硬核約束】**：本步驟完成前，必須明確前後端執行順序（並行/前端先行/後端先行），且必須單獨確認 `PROJECT_FRAME.md` 第 6 節的 **API/Mock 決策**，方可過關。 |
| **3** | **交互邏輯** | `AGENTS.md`, `FLOWS.md` | `PRD.md`, `PROJECT_FRAME.md`, `CONTRACT.md`(若有) | 定義頁面路由、狀態機與核心 UI 操作流程。產出 `FLOWS.md`。 |
| **4** | **前端交接包** | `AGENTS.md`, `FRONTEND_HANDOFF.md` | `PRD.md`, `PROJECT_FRAME.md`, `FLOWS.md` | 將上游設計濃縮。**交接包必須完全自包含（Self-contained）**，確保專業前端 AI 唯獨此檔即可開工。 |
| **5** | **專業前端編碼** | `AGENTS.md`, 專案源碼目錄 (如 `src/`) | `FRONTEND_HANDOFF.md`, `STATUS.md` | 依據交接包瘋狂輸出代碼，嚴禁修改上游任何 `.md` 設計文件。 |
| **6** | **項目記錄** | `AGENTS.md`, `STATUS.md`, `CHANGELOG.md`, `ERRORS.md` | 全專案檔案 | 整理本次對話的程式碼變更、版本號升級，並將踩坑、Bug 與修復方案詳細寫入 `ERRORS.md`。 |
| **7** | **驗收閘門** | `GATES.md`, `check.sh` | 全專案檔案 | 依據 `GATES.md` 撰寫自動化測試，在終端機執行 `bash check.sh` 必須 100% 通過。 |

*註：`WORKFLOW.md`（詳細流程說明）在任何步驟均為【全局唯讀】，僅在需要理解流程細節時由 AI 讀取。*

---

## ⚠️ 約束衝突與回退規範 (Rollback Rule)
當開發推進到後續步驟，發現底層約束衝突時，必須修改 `## 🚀 當前狀態` 並退回涉及的最早步驟，不可繞過：
- **互動設計超出技術邊界** $\rightarrow$ 退回 **第 2 步 (項目框架)** 重新評估技術架構。
- **技術約束影響產品範圍** $\rightarrow$ 退回 **第 1 步 (PRD 規劃)** 重新調整產品範疇。
*注意：退回後，該步驟下游的所有檔案重新變更為【鎖定】狀態，直到重新確認推進。*